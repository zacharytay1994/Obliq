using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

public class ZBossAttacks
{
    public static void PulseWave(float pulserate, int pulseamount, float pulsethickness, float pulsespeed, Collider2D player, Transform parent)
    {
        //GameObject executer = (GameObject)AssetDatabase.LoadAssetAtPath("Assets/Attacks/BossAttacks/AttackExecuter.prefab", typeof(GameObject));
        //GameObject temp = Object.Instantiate(executer, parent);
        //temp.GetComponent<AttackExecuter>().Init("pulsewave", player);
        //temp.GetComponent<AttackExecuter>().InitPulse(pulserate, pulseamount, pulsethickness, pulsespeed);
    }
}
